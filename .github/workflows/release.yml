# =============================================================================
# GitHub Actions DESATIVADO — use release local:
#   npm run release:version -- <versão>
#   npm run tauri:build:release
#   npm run release:publish -- <versão>
# Para reativar o CI: descomente este arquivo (remova o # de cada linha).
# =============================================================================
#
# name: Release

# on:
#   workflow_dispatch:
#   push:
#     branches:
#       - release
#       - 'release/**'
#     tags:
#       - 'easy-start-v*'

# permissions:
#   contents: write

# # Actions em Node 24 (Node 20 deprecado nos runners — jun/2026)
# env:
#   FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
#   CARGO_REGISTRIES_CRATES_IO_PROTOCOL: sparse
#   CARGO_INCREMENTAL: 0
#   CARGO_PROFILE_RELEASE_CODEGEN_UNITS: 32

# jobs:
#   publish:
#     runs-on: windows-latest

#     steps:
#       - uses: actions/checkout@v5

#       - name: Setup Node.js
#         uses: actions/setup-node@v5
#         with:
#           node-version: 22
#           cache: npm

#       - name: Cache Angular build
#         uses: actions/cache@v4
#         with:
#           path: .angular/cache
#           key: angular-${{ runner.os }}-${{ hashFiles('package-lock.json') }}-${{ hashFiles('tsconfig*.json', 'angular.json', 'src/**/*.ts') }}
#           restore-keys: |
#             angular-${{ runner.os }}-${{ hashFiles('package-lock.json') }}-
#             angular-${{ runner.os }}-

#       - name: Install Rust
#         uses: dtolnay/rust-toolchain@stable

#       - name: Rust cache
#         uses: swatinem/rust-cache@v2
#         with:
#           workspaces: src-tauri -> target
#           shared-key: release-windows
#           cache-on-failure: true
#           cache-workspace-crates: true

#       - name: Install frontend dependencies
#         run: npm ci

#       - name: Verificar chave de assinatura
#         shell: pwsh
#         env:
#           TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
#         run: |
#           if ([string]::IsNullOrWhiteSpace($env:TAURI_SIGNING_PRIVATE_KEY)) {
#             throw 'Secret TAURI_SIGNING_PRIVATE_KEY não configurado. Sem ela não há .sig nem latest.json.'
#           }
#           Write-Host 'Chave de assinatura presente.'

#       - uses: tauri-apps/tauri-action@v0
#         id: tauri
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
#           # Chave gerada com --ci usa senha vazia; sem isso o build pede password no runner.
#           TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ''
#         with:
#           tagName: easy-start-v__VERSION__
#           releaseName: Easy Start v__VERSION__
#           releaseBody: |
#             Instalador Windows e atualização automática para esta versão.

#             Baixe o instalador `.exe` nos assets abaixo.
#             O manifesto de atualização: https://github.com/vssoares/easy-start/releases/latest/download/latest.json
#           releaseDraft: false
#           prerelease: false
#           includeUpdaterJson: true
#           updaterJsonPreferNsis: true
#           retryAttempts: 1
#           args: --config src-tauri/tauri.ci.conf.json --ci

#       - name: Verificar latest.json no release
#         shell: pwsh
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: |
#           $tag = "easy-start-v${{ steps.tauri.outputs.appVersion }}"
#           $api = "https://api.github.com/repos/${{ github.repository }}/releases/tags/$tag"
#           $release = Invoke-RestMethod -Uri $api -Headers @{
#             Authorization = "Bearer $env:GH_TOKEN"
#             Accept = 'application/vnd.github+json'
#           }
#           $asset = $release.assets | Where-Object { $_.name -eq 'latest.json' }
#           if (-not $asset) {
#             throw "Asset latest.json ausente no release $tag"
#           }
#           Write-Host "Release OK: $($release.html_url)"
#           Write-Host "latest.json (tag): $($asset.browser_download_url)"

#       - name: Verificar URL pública do updater
#         shell: pwsh
#         run: |
#           $url = 'https://github.com/vssoares/easy-start/releases/latest/download/latest.json'
#           Write-Host "Testando: $url"
#           $r = Invoke-WebRequest -Uri $url -UseBasicParsing
#           if ($r.StatusCode -ne 200) {
#             throw "Manifesto público retornou $($r.StatusCode)"
#           }
#           $json = $r.Content | ConvertFrom-Json
#           Write-Host "Versão no manifesto: $($json.version)"
#           Write-Host 'Updater OK (GitHub Releases /latest/download/)'
